# Viewer dev defaults (see docs/DECISIONS-viewer.md DV-001/DV-002/DV-010).
# Shared database: the viewer branch is merged and its embedded migrator
# matches the shared schema (v3), so the DV-007 one-DB-per-worktree rule no
# longer applies; delver_viewer is legacy/disposable (DV-010).
DATABASE_URL=postgres://delver:delver@localhost:5433/delver
