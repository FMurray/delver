# Viewer dev defaults (see docs/DECISIONS-viewer.md DV-001/DV-002/DV-007).
# Dedicated database: parallel worktrees must not share one DB — the embedded
# migrator refuses migrations applied by other branches (DV-007).
DATABASE_URL=postgres://delver:delver@localhost:5433/delver_viewer
